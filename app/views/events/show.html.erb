<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Creator:</strong>
  <%= @event.user.name %>
  <%= link_to 'Show', @event.user %>
</p>

<%= form_for(current_user.active_rsvps.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :attended_event_id, @event.id %></div>
  <%= f.submit "Attend Event" %>
<% end %>